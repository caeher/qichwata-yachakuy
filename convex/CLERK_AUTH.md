# Clerk JWT for Convex (after Dashboard → JWT templates → Convex)
# CLERK_JWT_ISSUER_DOMAIN=https://your-instance.clerk.accounts.dev
#
# Then update convex/auth.config.ts providers and run `pnpm convex:dev`.
